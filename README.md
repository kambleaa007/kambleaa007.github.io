# website

### Staging

1. add --all

> > > git add --all
> > > git add .
> > > git add \*
> > > git add filename

### push

2. push

> > > git push -u origin master

### commit

3. commit

> > > git commit -m "Initial commit"

